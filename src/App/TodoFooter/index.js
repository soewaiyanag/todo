import Filter from 'App/Filter';
import StyledTodoFooter from './styled';

const TodoFooter = () => {
  return (
    <StyledTodoFooter>
      <Filter />
    </StyledTodoFooter>
  );
};

export default TodoFooter;
