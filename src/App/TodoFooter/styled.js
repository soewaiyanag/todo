import Box from 'shared/Box';
import styled from 'styled-components';

const StyledTodoFooter = styled(Box)`
  display: flex;
  gap: 1rem;
  justify-content: space-between;
`;

export default StyledTodoFooter;
