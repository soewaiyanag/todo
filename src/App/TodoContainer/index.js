import TodoItems from "../TodoItems";
import StyledTodoContainer from "./styled";

const TodoContainer = () => {
  return (
    <StyledTodoContainer>
      <TodoItems />
    </StyledTodoContainer>
  );
};

export default TodoContainer;
