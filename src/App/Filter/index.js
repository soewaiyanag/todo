import StyledFilter from './styled';

const Filter = () => {
  return (
    <StyledFilter>
      <button>All</button>
      <button>Active</button>
      <button>Completed</button>
    </StyledFilter>
  );
};

export default Filter;
