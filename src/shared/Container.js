import styled from "styled-components";

const px = "20px";

const Container = styled.div`
  max-width: 500px;
  margin-left: auto;
  margin-right: auto;
  padding-left: ${px};
  padding-right: ${px};
`;

export default Container;
